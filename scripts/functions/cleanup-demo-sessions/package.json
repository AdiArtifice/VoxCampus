{
  "name": "cleanup-demo-sessions",
  "version": "1.0.0",
  "description": "Appwrite function to clean up old demo session data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-appwrite": "^9.0.0"
  }
}